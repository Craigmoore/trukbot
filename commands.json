{
    "commands": {
        "^!help$": "Available commands: !streams, !help"
	},

    "owner_commands": {
        "^!quit$": "quit()"
    },

    "execs": {
        "^!song$": "sendmsg(\"Now playing: %s - %s (album: %s)\" % (lastfm.artistName.encode('utf-8'), lastfm.trackName.encode('utf-8'), lastfm.albumName.encode('utf-8')))",
        "https?://[\\w./?=]+": "doLinks(msg)"

   },

    "triggers": {
        "FrankerZ": "FrankerZ"
    }
}